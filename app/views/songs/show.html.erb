<h1>Songs#show</h1>
<%= link_to 'Add new song', new_artist_song_path(@artist) %>
<% @artist.songs.each do |songs| %>
<h4><%= song.name %></h4>
<h6><%= song.bpm %></h6>
<%= link_to 'edit', edit_artist_song_path(@artist, song) %>
<%= link_to 'delete', artist_song_path(@artist, song), method: :delete %>
<% end %>
